services:
  - type: web
    name: reloop-yolo
    env: python
    rootDir: reloop-yolo
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
